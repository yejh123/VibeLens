Analyze {{ session_count }} session{{ "s" if session_count != 1 else "" }} and select relevant skills for evolution.

{{ session_digest }}

## Installed Skills

{% for skill in installed_skills %}
- **{{ skill.name }}**: {{ skill.description }}
{% endfor %}

Select the skills most relevant to the observed patterns. Return their names in `relevant_skills`.
