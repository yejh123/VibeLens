Analyze {{ session_count }} coding agent session{{ "s" if session_count != 1 else "" }} to detect workflow patterns and recommend skills.

{{ session_digest }}

{% if installed_skills %}
## Installed Skills

The user already has these skills installed locally:
{% for skill in installed_skills %}

- [id={{ loop.index0 }}] **{{ skill.name }}**: {{ skill.description }}
{% endfor %}

Do not recommend skills that duplicate already-installed functionality.
{% endif %}

{% if skill_candidates %}
## Skill Candidates

Choose from these skills to recommend. Only recommend skills that genuinely match detected patterns:
{% for candidate in skill_candidates %}

- [id={{ loop.index0 }}] **{{ candidate.name }}**: {{ candidate.summary }}{% if candidate.tags %} [{{ candidate.tags | join(", ") }}]{% endif %}
{% endfor %}
{% endif %}

Provide `title`, `user_profile`, workflow patterns with real `example_refs`, `summary`, and recommendations from the candidate list.
