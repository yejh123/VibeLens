Analyze {{ session_count }} coding agent session{{ "s" if session_count != 1 else "" }} to detect workflow patterns and propose new skills.

{{ session_digest }}

{% if installed_skills %}
## Installed Skills

The user already has these skills installed. Do NOT propose skills that duplicate this functionality:
{% for skill in installed_skills %}

- [id={{ loop.index0 }}] **{{ skill.name }}**: {{ skill.description }}
{% endfor %}
{% endif %}

Provide `title`, `user_profile`, workflow patterns with real `example_refs`, `summary`, and skill proposals.
