Generate a complete SKILL.md for the following approved proposal.

## Proposal

- **Name**: {{ proposal_name }}
- **Description**: {{ proposal_description }}
- **Rationale**: {{ proposal_rationale }}
{% if addressed_patterns %}
- **Addressed Patterns**: {{ addressed_patterns | join(", ") }}
{% endif %}

## Session Evidence

{{ session_digest }}

{% if installed_skills %}
## Installed Skills

The user already has these skills. Ensure the new skill does not duplicate them:
{% for skill in installed_skills %}

- [id={{ loop.index0 }}] **{{ skill.name }}**: {{ skill.description }}
{% endfor %}
{% endif %}

Use the proposal name as `name`. Include complete YAML frontmatter and a markdown body with step-by-step instructions (1,500-2,000 words, imperative form, with negative cases) in `skill_md_content`.
