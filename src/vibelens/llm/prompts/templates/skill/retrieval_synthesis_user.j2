Merge retrieval results from {{ batch_count }} batches analyzing {{ session_count }} total sessions.

{% for batch in batch_results %}
## Batch {{ loop.index }}: {{ batch.title }}

### Workflow Patterns
{% for pattern in batch.workflow_patterns %}
- **{{ pattern.title }}**: {{ pattern.description }} (refs: {% for ref in pattern.example_refs %}{{ ref }}{% if not loop.last %}, {% endif %}{% endfor %})
{% endfor %}

### Recommendations
{% for rec in batch.recommendations %}
- **{{ rec.skill_name }}**: {{ rec.rationale }} (confidence: {{ rec.confidence }}, patterns: {{ rec.addressed_patterns | join(", ") }})
{% endfor %}

{% endfor %}

Produce a single merged result. Deduplicate workflow patterns by merging `example_refs`. Rank recommendations by confidence and frequency. Include a self-explanatory `title`.
