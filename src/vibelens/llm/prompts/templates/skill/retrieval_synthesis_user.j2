Merge retrieval results from {{ batch_count }} batches analyzing {{ session_count }} total sessions.

{% for batch in batch_results %}
## Batch {{ loop.index }}: {{ batch.title }}

### Summary
{{ batch.summary }}

### User Profile
{{ batch.user_profile }}

### Workflow Patterns
{% for pattern in batch.workflow_patterns %}
- **{{ pattern.title }}**: {{ pattern.description }}
{% endfor %}

### Recommendations
{% for rec in batch.recommendations %}
- **{{ rec.skill_name }}**: {{ rec.rationale }} (confidence: {{ rec.confidence }}, patterns: {{ rec.addressed_patterns | join(", ") }})
{% endfor %}

{% endfor %}

Produce a single merged result with:
- `workflow_patterns`: Deduplicated patterns with merged example_refs.
- `recommendations`: Top 1-10 recommendations ranked by confidence and frequency.
- `summary`: One-sentence conclusion + 2-4 bullet points starting with '- '. Max 100 words.
- `user_profile`: Merged user profile.