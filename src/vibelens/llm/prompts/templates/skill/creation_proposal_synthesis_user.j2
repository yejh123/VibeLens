Merge proposals from {{ batch_count }} batches analyzing {{ session_count }} total sessions.

{% for batch in batch_results %}
## Batch {{ loop.index }}: {{ batch.title }}

### Summary
{{ batch.summary }}

### User Profile
{{ batch.user_profile }}

### Workflow Patterns
{% for pattern in batch.workflow_patterns %}
- **{{ pattern.title }}**: {{ pattern.description }}
{% endfor %}

### Proposals
{% for proposal in batch.proposals %}
- **{{ proposal.skill_name }}**: {{ proposal.description }}
  - Rationale: {{ proposal.rationale }}
  - Addresses: {{ proposal.addressed_patterns | join(", ") }}
{% endfor %}

{% endfor %}

Produce a single merged result. Deduplicate workflow patterns by merging `example_refs`. Rank proposals by frequency and quality. Include `title`, `user_profile`, and `summary`.
