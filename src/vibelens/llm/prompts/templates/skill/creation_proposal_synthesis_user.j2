Merge proposals from {{ batch_count }} batches analyzing {{ session_count }} total sessions.

{% for batch in batch_results %}
## Batch {{ loop.index }}: {{ batch.title }}

### Summary
{{ batch.summary }}

### User Profile
{{ batch.user_profile }}

### Workflow Patterns
{% for pattern in batch.workflow_patterns %}
- **{{ pattern.title }}**: {{ pattern.description }}
{% endfor %}

### Proposals
{% for proposal in batch.proposals %}
- **{{ proposal.skill_name }}**: {{ proposal.description }}
  - Rationale: {{ proposal.rationale }}
  - Addresses: {{ proposal.addressed_patterns | join(", ") }}
{% endfor %}

{% endfor %}

Produce a single merged result with:
- `workflow_patterns`: At most 5 deduplicated patterns with merged example_refs.
- `proposals`: Top 1-5 proposals ranked by frequency and quality.
- `summary`: One-sentence conclusion + 2-4 bullet points starting with '- '. Max 100 words.
- `user_profile`: Merged user profile.