Merge proposals from {{ batch_count }} batches analyzing {{ session_count }} total sessions.

{% for batch in batch_results %}
## Batch {{ loop.index }}

### Summary
{{ batch.summary }}

### User Profile
{{ batch.user_profile }}

### Workflow Patterns
{% for pattern in batch.workflow_patterns %}
- **{{ pattern.title }}**: {{ pattern.description }} (pain: {{ pattern.pain_point }})
{% endfor %}

### Proposals
{% for proposal in batch.proposals %}
- **{{ proposal.name }}**: {{ proposal.description }}
  - Rationale: {{ proposal.rationale }}
  - Addresses: {{ proposal.addressed_patterns | join(", ") }}
{% endfor %}

{% endfor %}

## Output Schema

{{ output_schema }}

Produce a single merged result with:
- `workflow_patterns`: Deduplicated patterns with merged example_refs.
- `proposals`: Top 1-8 proposals ranked by frequency and quality.
- `summary`: Unified narrative overview.
- `user_profile`: Merged user profile.